/**
 * Route group manifest — architecture only.
 * Maps this App Router group to the architecture blueprint.
 */
export const routeGroupManifest = {
  id: "(public)",
  layout: "public-website",
  portal: "public",
  manifestId: "public",
} as const;
