import { OperationsComingSoonPage } from "@/features/operations";

export default function OperationsWorkersRoute() {
  return (
    <OperationsComingSoonPage
      moduleName="Workers"
      moduleDescription="Background worker orchestration, retries, and operational queue controls will be introduced as dedicated SOC worker modules."
    />
  );
}
